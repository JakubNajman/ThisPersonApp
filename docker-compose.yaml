services:
  django:
    build:
      context: app
    ports:
      - "8000:8000"
    command: python manage.py runserver 0.0.0.0:8000
    environment:
      CONN_STR: mongodb://user:pass@mongodb
    # networks:
    #   - node-network

networks:
  node-network:
    driver: bridge